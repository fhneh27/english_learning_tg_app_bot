# frontend service → Settings → Config-as-code → railway.frontend.toml
# Root Directory must be EMPTY (repo root). See docs/RAILWAY.md if build fails.
[build]
builder = "DOCKERFILE"
dockerfilePath = "frontend/Dockerfile.prod"

[deploy]
restartPolicyType = "ON_FAILURE"
